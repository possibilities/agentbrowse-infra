# 0002: Offer Hypeman alongside Apple container

Hypeman is an explicitly managed local or remote VM runtime for the same OCI browser workload. Infrastructure owns installation, service lifecycle and remote network forwarding; Agentbrowse owns instances and durable profile volumes. Stopping Hypeman preserves its data so a demo or ordinary shutdown never erases authentication state.
